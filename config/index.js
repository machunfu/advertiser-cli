/**
 * files中的属性与文件中被替换的标识保持一致
 */

const files = {
    // 媒体编码
    mediaCode: [{ fileName: 'package.json', filePath: 'marathon/package.json' }],
    // 媒体编码
    mediaName: [{ fileName: 'package.json', filePath: 'marathon/package.json' }],
    // 端口号
    port: [{ fileName: 'package.json', filePath: 'marathon/package.json' }]
}

module.exports = {
    files
}